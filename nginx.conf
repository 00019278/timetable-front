server {
    listen 80;
    server_name _;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # Перенаправляем все запросы на index.html для корректной работы Vue Router
        try_files $uri $uri/ /index.html;
    }
}